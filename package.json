{
  "name": "sky130-asic-puzzle",
  "description": "Reverse-engineering canary for klayout-tools — warm-up regression harness",
  "private": true,
  "scripts": {
    "worktree:return": "./.loom/scripts/worktree-return.sh",
    "fetch": "./scripts/fetch-puzzle.sh",
    "lint": "./scripts/lint.sh",
    "test": "./tools/test-compare && ./tools/test-inventory && ./tools/test-pins",
    "regression": "./scripts/warmup-regression.sh",
    "embargo": "./scripts/check-embargo.sh",
    "check:ci": "npm run lint && npm run regression && npm run embargo",
    "check:all": "npm run check:ci"
  }
}
